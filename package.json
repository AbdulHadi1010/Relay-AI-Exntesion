{
  "name": "relay-chrome-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Relay Chrome Extension - Never hit an AI dead end again",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.1",
    "fast-check": "^4.1.1",
    "jsdom": "^25.0.1",
    "vitest": "^3.2.1"
  }
}
